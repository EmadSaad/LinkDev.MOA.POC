import { AuthRoutingModule } from './auth-routing-module';

describe('AuthRoutingModule.Ts', () => {
  it('should create an instance', () => {
    expect(new AuthRoutingModule()).toBeTruthy();
  });
});
